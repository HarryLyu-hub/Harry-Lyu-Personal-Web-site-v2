# 约定A (Protocol A) - 需求确认与系统迭代开发规范

此文件为与吕老师（User）达成长期一致的开发协同契约。在后续任何版本修正或增量迭代中，AI 编码代理必须严格遵守并在首轮思考中高优先级遵循本文件内的全部细则：

---

### 第一条：【先对齐、后编程】需求对齐前置原则
- **核心逻辑**：当用户提出任何新的需求、建议或界面整改意见时，**严禁**立即着手修改任何代码或运行后台编译。
- **协同流程**：
  1. **步骤一（AI 解析）**：AI 代理必须首先将用户的需求逐条分解，在对话中以清晰的序号条目展示自己对“每一条需求”的理解、拟实施逻辑和建设性建议（不运行任何代码工单）。
  2. **步骤二（用户确认）**：等待用户进行逐条核实反馈（例如：“第一条没问题，第二条理解有偏差应该如此这般...”）。
  3. **步骤三（对准落笔）**：仅在双方彻底澄清无误、用户下达“执行编程修改”的明确指令后，AI 代理再一并调取文件编辑工具执行严密修改。防止因频繁自作主张、盲目编码带来长时间的盲盒运转与来回纠偏。

---

### 第二条：【存量锁定】非讨论模块绝对维持原状
- **核心逻辑**：在进行后续新增议题讨论时，严禁主动干扰、篡改或倒退那些此前已确认定型、运行正常的已有模块资产。
- **重点保护资产（包含但不仅限于）**：
  1. **活动会场真实实况照片**：必须固定指向已经成功持久化的真实物理照片路径（如 `/src/assets/images/keynote_uploaded_1782054741543.jpeg` 等系列文件），不得以任何理由自动回滚或生成 AI 假图。
  2. **书籍专著直通推荐模块**：以极简的官方签名版新书《出海制胜》及附赠大礼包 QR 连接直接引流的纯图片预览模式作为稳定版，不再擅自向表单交互退化。
  3. **其他未指名修改的导航、关于我们、个人履历等稳定运行模块**。
- **自查原则**：未在当前对齐范围内的代码行，严格不改、不碰，确保系统开发稳定性，告别“改一处、漏十处”的反复折腾。

---

### 第三条：【自查与模拟测试】杜绝低阶感官错误
- **核心逻辑**：AI 代理在将编译好后的成果交付给用户审阅前，必须通过测试自检思维对界面渲染可能出现的表现形式进行多维度研判、静态走查。
- **覆盖要点**
  - 点击跳转与渲染联动是否缺失空态？
  - 是否有明显的语法拼写引起白屏阻断？
  - 界面排版有无严重的重叠、断行或英文排版溢出？
- **检验标准**：保证递交给用户首屏验证的数据和样式，已经过开发者心智模拟流程检验，不出现低级、第一眼可见的交互漏洞或数据为空的异常界貌。

---

# 约定B (Protocol B) - 媒体资产协作与上传规范

针对沙盒容器临时性较强、大Base64数据极易损毁的客观技术局限，为彻底解决因“动态图片上载”导致的图片变黑、格式损坏和代码臃肿问题（约占历史调优时间的35%），特制订本协议：

### 第一条：【杜绝复杂动态上传代码】
- 针对长效、高精度的专业成果图片（如各工单案例、工坊配图、合影照片等），不再在前端和后端代码中开发复杂的动态上传、二进制转 Base64 数据库同步逻辑。
- 采用**静态存储、声明引用**的方式，直接对固定静态物理路径进行维护。

### 第二条：【AI 发起规格指令 → 用户文件上传协作机制】
- 当系统需要更换或新增任何图片/视频时，AI 代理不应去猜测或通过小弹窗收集，而是应当在对话中明确列出所需媒体的**技术规格单**：
  1. **文件名**（例如：`customer_experience_roadmap.png`）
  2. **推荐分辨率与尺寸比例**（例如：16:9，1200x675）
  3. **存放的目标物理目录**（默认路径均为：`/src/assets/images/`）
- **吕老师（User）侧协作**：吕老师在收到规格说明后，通过侧边栏文件树或者对话框附件，将图片直接以指定的命名上传至对应的物理目录。
- **AI 代理（Coder）侧协作**：AI 代理直接在代码中通过 `src="/src/assets/images/xxx"` 来完成对该图片的静态加载，避免任何运行时的动态保存转换过程。

### 第三条：【自愈与容错降级方案】
- 一旦用户或 AI 发现界面中存在图片白屏或黑屏（如“客户体验建设路线图工坊”的图片由于历史脏数据或临时断联黑掉），双方绝不惊慌重写大片代码。
- **处理方式**：AI 检查对应图片的物理文件名（如 `customer_experience_roadmap.png` 或 `outbound_road_bridge_1781665312853.jpg`），在代码里固定指向它。并由 AI 指引吕老师通过侧边栏重置/再次上传一张格式正确的同名图片。这一简单的覆盖重命名操作能在秒级完成，且具有 100% 的抗损坏和自愈能力。

---

# 称谓约定 (Greeting Convention)
- **核心逻辑**：AI 代理在后续所有对话中，应尊称用户为**“吕老师”**，体现学术与行业老兵的专业敬意，不再使用“吕先生”或任何其他机械式的称呼，营造亲切、高效、互信的协同氛围。
