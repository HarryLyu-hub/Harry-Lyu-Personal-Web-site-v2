# 跨国出海 CX/HR 导师工作室 - 数据库与系统功能测试用例 (Test Cases)

本文档专为 **吕老师** 整理，用于验证系统在迁移到 Ubuntu 服务器后，数据库、云端 Firestore 同步、API 引擎以及 AI 交互功能是否完全合规、交互正常。

---

## 📋 模块一：学子注册与 Firebase 云端双重同步校验
* **测试目的**：验证学子信息录入、本地存储和云端 Firestore 数据库的持久化双向对齐。
* **合规要求**：敏感密码字段加星号或不泄露；不含脏数据；支持离线与云端双态自愈。

### 【测试步骤】
1. 访问网站，在顶部导航选择 **「出海文化地图沙盒」** 或进入学员入口。
2. 点击 **「新学子注册 / 申请加入」** 按钮，弹出注册表单。
3. 输入正面测试数据：
   - **姓名/英文名**：`张建国 (Jay)`
   - **机构/公司**：`出海领航科技`
   - **邮箱/登录凭证**：`trainee_test@outbound.com`
   - **手机号码**：`13888888888`
   - **选择订阅期**：`1mo` (一个月)
   - **密码**：`Test123456`
4. 点击 **「提交注册申请」**。

### 【预期正面交互结果】
* **界面表现**：注册成功后，立即弹出精美的提示框（“注册申请已成功提交，正在为您激活云端同步”）。
* **本地校验**：
  * 打开浏览器开发者工具 (F12) -> Application -> Local Storage。
  * 检查 `HARRY_REGISTERED_STUDENTS` 键，应包含刚才录入的 `trainee_test@outbound.com` 对象。
* **云端持久化**（若配置了 Firebase）：
  * 检查终端后台日志，应打印：`Successfully persisted profile for 张建国 (Jay) to Firestore.`。
  * 云端 Firestore `students` 集合中会自动建立对应的文档。

---

## 📋 模块二：学子专属海外工作台登录与仪式感校验
* **测试目的**：验证登录验证逻辑，以及登录后学子工作台界面联动渲染是否缺失空态。
* **合规要求**：密码匹配无误，登录成功后顶部有实时在线呼吸灯并挂载代号，建立沉浸式仪式感。

### 【测试步骤】
1. 在顶部导航点击 **「关于吕华」** 页面下的学子激活入口，或直接在控制栏点击登录。
2. 输入刚才注册的邮箱 `trainee_test@outbound.com` 及密码 `Test123456`。
3. 点击 **「安全登录 / 激活工作台」**。

### 【预期正面交互结果】
* **界面表现**：
  * 登录弹窗平滑淡出。
  * 页面顶部顶栏会点亮一盏 **「绿宝石状的实时在线呼吸灯」**，并显示：`「学员 ID: trainee_test」已在线`。
  * 成功解锁只有学子可见的专属高阶案例与白皮书下载。

---

## 📋 模块三：交互式反馈引擎读写校验 (Local JSON DB)
* **测试目的**：验证学子及访客的意见反馈能够正确写入服务端的 `src/feedback.json`，并且能被 AI 引擎读取回复。
* **合规要求**：空数据不予提交，输入正面建议正常追加，不损坏已有历史记录（如默认的 fb-1 数据）。

### 【测试步骤】
1. 滚动页面到最底部，或点击导航中的 **「定制咨询/联系」**。
2. 找到 **「学子建言与网站改进意见」** 交互表格。
3. 选择反馈类别：`「沙盒功能优化」`。
4. 输入正面测试意见：`“希望能支持一键导出高管诊断雷达图为PDF格式，方便内部汇报。”`
5. 点击 **「提交反馈意见」** 按钮。

### 【预期正面交互结果】
* **即时交互**：表格上方或下方即时追加一条记录，状态标记为 `[Open/处理中]`。
* **服务端校验**：
  * 服务端 `src/feedback.json` 文件应被写入刚才的信息，其 `reply` 字段被 AI 助手自动占位，提示在下一次代理执行时可一键修改。
  * 数据没有黑屏或格式损坏。

---

## 📋 模块四：AI 跨文化冲突雷达图与碰撞分析校验
* **测试目的**：验证基于《文化地图》八大维度的真实案例碰撞是否正确计算，并且 Gemini AI 是否能合规输出。
* **合规要求**：输入案例不为空，AI 在 5-10 秒内输出极具深度、契合《文化地图》框架的八维度雷达图对比和碰撞深度分析。

### 【测试步骤】
1. 在 **「出海文化地图沙盒」** 页面中，选择预设经典案例（例如：`“巴哈萨黑盒之痛（印尼 VS 中国）”`）或在输入框中输入自建冲突。
2. 点击 **「一键发起深度文化冲突碰撞分析」**。

### 【预期正面交互结果】
* **雷达图渲染**：网页中会渲染出高对比度的文化差异雷达对比图（如印尼在“沟通”维度的 8.5 高语境对比中国等）。
* **冲突解析**：下方流畅渲染出大段由真实业务专家视角的定性定量剖析。

---

## 📋 模块五：学子分级权限与沙盒功能硬度控制测试
* **测试目的**：验证「普通游客（未登录/未授权）」与「激活学子账号（已登录）」之间存在安全的分级边界，绝无越权漏洞。
* **合规要求**：未登录的访客无法越权使用高级出海国别分析、多国雷达比对（上限 2 国），且无法发起自定义摩擦沙盒诊断。

### 【测试步骤（游客越权阻断测试）】
1. 退出任何当前已登录的账号（在顶部右上角菜单「菜单 (Menu)」内点击 **「退出登录」**）。
2. 在 **「出海文化地图沙盒」** 中，尝试勾选第三个国家进行多国雷达大比对（例如：在已有“中国”、“美国”的基础上，尝试勾选“法国”）。
3. 滚动到 **「自定义跨国摩擦沙盒诊疗所」** (MODULE 3: Student Sandbox Workspace Clinic)。
4. 尝试输入咨询标题与详细描述，然后点击提交诊断。

### 【预期正面/拦截交互结果】
* **雷达图限额拦截**：
  * 当勾选第 3 个国别标签时，系统会立即拦截并弹出提示：`“🔒 游客单次限比对2国。立即免费注册/登入专属出海学子通道...”`
  * 阻断其越权，并自动弹出「学子中心登录/注册窗口」。
* **沙盒诊疗所硬度锁**：
  * 沙盒诊疗界面呈现深色半透明加锁状态，显示：`“🔒 自定义跨国摩擦沙盒诊疗所 已加锁...”`
  * 所有的输入框、提交按钮均为 disabled（禁用）不可点击态，右侧直接配备 **「立即一键注册登入学子通道」** 的特许权引导，完全隔绝非法流量。

---

## 📋 模块六：SaaS 订阅卡激活、过期拦截与续费提醒校验
* **测试目的**：验证学子在购买不同长短期的 SaaS 订阅后，账户能在有效期内完美运转；当即将到期（≤7天）时，能实时点亮“黄牌”预警；若已经到期，则瞬间执行“红牌”权限熔断。
* **合规要求**：系统通过实时时间戳与内置账户（或云端 profile）的 `expiryDate` 毫秒数做精确校验，并在学子卡片上显示醒目催费入口。

### 【场景 A 测试：正常到期续费预警（以“三节课高管”为例）】
1. 点击顶部右上角 **「菜单 (Menu)」** -> **「登录 / 注册」**。
2. 在 **「一键快捷登录」** 列表中，直接点击 **「李飞 (Sanjieke)」**（绑定邮箱为 `student@sanjieke.cn`）。
3. *注*：由于该临时账号内置有效期仅剩约 12 小时（处于 ≤7 天的预警区间内）。
4. **【预期交互结果】**：
   * 登录成功后，再次点击右上角 **「菜单 (Menu)」**。
   * **「学子中心 (STUDENT PORTAL)」** 会自动展现高对比度的 **「琥珀金(Amber) 警告卡片」**：
     * 明确提示：`“⚠️ 学员特许到期续约提醒”`。
     * 倒计时计算：`“账户专享特权仅剩 12 小时。到期后高级出海雷达与诊断锁将关闭。”`
     * 预置一个闪烁的快捷催续入口：**「联系助教闪电续期 ⚡」**，点击一键跳转到文案咨询卡。

### 【场景 B 测试：到期自动熔断拦截（以“赵过期”为例）】
1. 在顶部右上角菜单中点击 **「退出登录」**。
2. 再次点击 **「登录 / 注册」**，在快捷登录列表中直接选择 **「赵过期 (过期续费演示)」**（绑定邮箱为 `expired_demo@global.com`）。
3. *注*：由于该账号的 `expiryDate` 设置为已过期 2 天。
4. **【预期交互结果】**：
   * 登录成功后，系统会进行时间戳秒级静默比对，判定过期并把状态实时标为 `"expired"`。
   * 点击右上角 **「菜单 (Menu)」** 展开，可以看到学子卡片呈现醒目的 **「红玫瑰(Rose) 过期锁定状态」**：
     * 显示：`“⚠️ 专属出海包期卡已到期”`
     * 提示：`“您的学员专享特许期已结束。出海诊疗所与德国、英国、拉美等高级维度图谱已锁定，请联系助教续约。”`
   * 此时再次折返到 **「出海文化地图沙盒」** 或 **「自定义沙盒诊疗所」**，系统已判定其 `isTraineeActive` 为 `false`：
     * 尝试展开除“中国、美国”以外的隐藏国别，均被强制拦截。
     * 自定义诊疗所被锁定加灰，从而以最严格、零滞后的优雅方式保护商业 SaaS 软件特许的完整商业闭环。

---

## 🔍 服务器自我维护与升级检查清单
当您在 Ubuntu 环境中拉取更新后，可运行以下命令验证 Nginx 与 Node.js 的状态：

1. **服务运行状态检查**：
   ```bash
   sudo systemctl status nginx
   pm2 status || ps aux | grep node
   ```
2. **测试配置文件是否有语法错误**：
   ```bash
   sudo nginx -t
   ```
3. **查看 Nginx 访问和错误日志**：
   ```bash
   tail -n 20 /var/log/nginx/error.log
   ```
